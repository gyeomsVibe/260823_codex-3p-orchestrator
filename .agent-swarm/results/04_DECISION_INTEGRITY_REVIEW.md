# 의사결정 무결성 재검토와 최소 수정

실측: 2026-09-06 02:55 KST. 작업: G0-CRITIC-OPTIMIZE.

## 비판 판정

1. **높음 — 미등록 투표자가 정족수를 만든다.** 기준은 등록된 세 구성원의 과반이다. `csc_decide.decide`가 입력 사전 전체를 집계해 ghost1/ghost2의 AGREE만으로 APPROVED를 반환했다. 실제 구성원의 동의 없이 승인이 성립하므로 구성원과 표 값 검증을 집계 전에 추가했다. 인증된 발신자인지 또는 같은 후보의 표인지는 이 함수가 아직 보증하지 않는다.
2. **높음 — 감시판이 현재 안건의 표를 만들어낸다.** `csc_dashboard.checks`가 Claude 찬성과 3시간 전 시작 시각을 고정했고, 발신이 오래됐다는 이유로 다른 도구를 꺼짐으로 분류했다. 실제 표결과 무관한 판단이므로 해당 합성 판정을 제거하고 '판정 미검증'·사유·다음 행동·원문 경로를 표시한다. 실제 안건별 표결 연결은 후속 범위다.
3. **중요 — 이전 Codex의 대기 종료와 인계가 부족했다.** 과거 미응답을 끝없는 차단으로 유지하면 진행하지 못한다. 새 기권·마감 정책은 보존한다. 실반대와 기권은 구분하며, 과거 후보의 찬성은 새 후보 승인으로 재사용하지 않는다.

## 최적화 기준과 결과

- 목적: 잘못된 입력이나 화면의 가정으로 진행 승인을 만들지 않기.
- 기준선: 기존 unittest 99개 통과에도 가짜 투표자 두 명이 APPROVED를 생성했다.
- 변경 범위: csc_decide.py 입력 검증, csc_dashboard.py의 합성 판정 제거, tests/test_decision_integrity.py 회귀 검사 5개.
- 보존: 기존 마감, 기권, 정족수, 반대 처리, 사용자 승인 경계. 원문 JSONL과 기존 타 세션 변경은 보존했다.
- 변경 후: unittest 104/104, decide 자체검증 12/12, 사용자 원칙 동기화 5개 파일, dashboard --check 모두 종료 코드 0.
- 브라우저 시각 검사는 수행하지 않았다. 렌더링 관련 기존 자동 테스트는 전체 검사에 포함됐다.

## 다음 단계와 승인 경계

Claude 읽기 전용 검토 요청: MSG-20260905-175350-075331-04f7c04c-COD-CLA. 이 기록 시점에는 응답 대기다. 원문은 `.agent-swarm/messages/queue.jsonl`에 있다. 피어 응답 없이 합의 완료를 주장하지 않는다.

다음은 피어 반론 처리 → 편집·스테이징 동결 협의 → 같은 후보 해시로 후보 검토(C0/C1) 순서다. 다른 세션의 스테이징 변화가 관측되어 이번에는 최종 후보 해시를 확정하지 않았다. commit·push는 실행하지 않았다. 지금 사용자가 실행하거나 승인할 일은 없다.

후속 실측 02:57 KST: 02:53:50 발신 후 3분 마감 경과에도 Claude 답변 미수신. 제한 대기는 종료 코드 1(timeout)이었으며 찬성·실반대로 해석하지 않는다. 규약 13절에 따라 이 검토의 미응답은 기권으로 기록하며 재요청하지 않는다. 첫 단계의 로컬 수정·자동검증은 완료, 후보 합의는 미완료다. 최종 diff --check는 종료 코드 0이다.
